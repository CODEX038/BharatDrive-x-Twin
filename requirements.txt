# Core: stdlib-only. Everything below is optional and feature-gated.
# --- recommended ---
numpy>=1.23
opencv-python>=4.7        # camera capture, head pose (solvePnP), image quality metrics
# --- landmark backends (pick one; face_recognition already lives in your dds01env venv) ---
# face_recognition==1.3.0 ; dlib required
# mediapipe>=0.10
# --- road detection (optional) ---
# ultralytics>=8.0
# --- audio alerts (optional; alarm falls back to console/dashboard) ---
# pygame>=2.5
# --- evaluation phase (optional) ---
# scikit-learn>=1.3
# --- tests ---
pytest>=7.0
